<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/coffee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/coffee"
        android:supportsRtl="true"
        android:theme="@style/Theme.VNCoffee"
        tools:targetApi="31">
        <activity android:name=".Activities.DetailStatisticActivity"></activity>
        <activity android:name=".Activities.AddAccActivity"></activity>
        <activity android:name=".Activities.PasswordActivity"></activity>
        <activity
            android:name=".Activities.EditTableActivity"
            android:label="Sửa thông tin bàn"
            android:theme="@style/PopUpActivity"></activity>
        <activity android:name=".Activities.PaymentActivity"></activity>
        <activity
            android:name=".Activities.AmountMenuActivity"
            android:label="Số lượng món"
            android:theme="@style/PopUpActivity"></activity>
        <activity android:name=".Activities.AddTypeActivity"></activity>
        <activity android:name=".Activities.AddMenuActivity"></activity>
        <activity
            android:name=".Activities.AddTableActivity"
            android:label="Thêm thông tin bàn"
            android:theme="@style/PopUpActivity"></activity>
        <activity android:name=".Activities.HomeActivity"></activity>
        <activity android:name=".Activities.StatisticsActivity"></activity>


        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>